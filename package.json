{
  "name": "deep-galaxy",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:scanner": "npm run build --workspace=packages/scanner",
    "scan": "node packages/scanner/dist/index.js",
    "dev": "npm run dev --workspace=packages/visualizer"
  }
}
