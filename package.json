{
  "name": "deep-galaxy",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "scan": "npm run build --workspace=packages/scanner",
    "dev": "npm run dev --workspace=packages/visualizer"
  }
}
